<?xml version="1.0" encoding="utf-8"?>
<!-- No sources: packing a library with no dependencies must not need the
     network, and a source here would make the test depend on one. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
