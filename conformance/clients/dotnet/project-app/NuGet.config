<?xml version="1.0" encoding="utf-8"?>
<!-- Sources come from the command line (-s), so the same project restores
     from a hosted feed or from a group without editing a file. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
