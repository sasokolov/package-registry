---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "registry_admin_binding Resource - Package Registry"
subcategory: ""
description: |-
  One identity pattern allowed to administer the registry: to read and change the configuration document, issue and revoke tokens, and quarantine coordinates.
  Each binding owns exactly one pattern and leaves the rest of the list alone, so several teams can manage their own access without rewriting each other's.
---

# registry_admin_binding (Resource)

One identity pattern allowed to administer the registry: to read and change the configuration document, issue and revoke tokens, and quarantine coordinates.

Each binding owns exactly one pattern and leaves the rest of the list alone, so several teams can manage their own access without rewriting each other's.

## Example Usage

```terraform
# Anyone in the infra group's CI may administer this site.
resource "registry_admin_binding" "infra" {
  pattern = "project:infra/*"
}

# As may a named operator token.
resource "registry_admin_binding" "oncall" {
  pattern = "token:ops-oncall"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `pattern` (String) Identity pattern, in the same language as a feed's publishers: `token:ops-*`, `project:infra/*`, `sub:...`. Editing it is a different binding, so the old one is removed and the new one added.

### Read-Only

- `id` (String) The pattern, which is its identity.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Bindings are imported by the pattern itself.
terraform import 'registry_admin_binding.infra' 'project:infra/*'
```
