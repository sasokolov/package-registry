node_modules/
*.tsbuildinfo

# Build output is generated by `make ui`, never committed. The placeholder
# keeps the go:embed target present so `go build ./...` works without Node.
dist/*
!dist/.gitkeep
