---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "registry_oidc_issuer Resource - Package Registry"
subcategory: ""
description: |-
  An OIDC issuer whose id_tokens this site accepts as identities — typically a GitLab instance issuing CI job tokens. Adding one lets pipelines authenticate without a static secret to leak.
---

# registry_oidc_issuer (Resource)

An OIDC issuer whose id_tokens this site accepts as identities — typically a GitLab instance issuing CI job tokens. Adding one lets pipelines authenticate without a static secret to leak.

## Example Usage

```terraform
# Accept GitLab CI id_tokens, so pipelines authenticate without a static
# secret that could leak.
resource "registry_oidc_issuer" "gitlab" {
  issuer   = "https://gitlab.example.com"
  audience = "package-registry"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `audience` (String) Required `aud` claim. Tokens minted for anything else are refused.
- `issuer` (String) Expected `iss` claim, e.g. `https://gitlab.com`. Trusting a different issuer is a different trust decision, so changing it replaces the resource.

### Optional

- `jwks_url` (String) Explicit JWKS endpoint. Leave unset to let the registry discover it from the issuer.

### Read-Only

- `id` (String) The issuer URL, which is its identity.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Issuers are imported by issuer URL.
terraform import registry_oidc_issuer.gitlab https://gitlab.example.com
```
